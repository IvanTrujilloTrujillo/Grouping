import { ReviewWithUserName } from './review-with-user-name';

describe('ReviewWithUserName', () => {
  it('should create an instance', () => {
    expect(new ReviewWithUserName()).toBeTruthy();
  });
});
